<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Modern Odoo 17 Asset Bundle for odoo_crm_dashboard -->
    <template id="assets_backend" inherit_id="web.assets_backend">
        <!-- JavaScript Files - Specific files to avoid conflicts -->
        <script type="text/javascript" src="/odoo_crm_dashboard/static/src/js/crm_dashboard_legacy_fixed.js"/>

        <!-- CSS Files -->
        <link rel="stylesheet" type="text/scss" src="/odoo_crm_dashboard/static/src/scss/*.scss"/>
        <link rel="stylesheet" type="text/css" src="/odoo_crm_dashboard/static/src/css/*.css"/>
    </template>

    <!-- Frontend Assets (if needed) -->
    <template id="assets_frontend" inherit_id="web.assets_frontend">
        <script type="text/javascript" src="/odoo_crm_dashboard/static/src/js/frontend.js"/>
        <link rel="stylesheet" type="text/css" src="/odoo_crm_dashboard/static/src/css/frontend.css"/>
    </template>

    <!-- QWeb Templates -->
    <template id="dashboard_template" name="odoo_crm_dashboard.Dashboard">
        <div class="o_odoo-crm-dashboard_dashboard">
            <div class="o_dashboard_header">
                <h2>OSUS Properties Dashboard</h2>
                <div class="o_dashboard_controls">
                    <button class="btn btn-primary" t-on-click="onRefreshData">
                        <i class="fa fa-refresh"/>
 Refresh
                    </button>
                </div>
            </div>
            <div class="o_dashboard_content">
                <t t-if="state.isLoading">
                    <div class="text-center p-4">
                        <i class="fa fa-spinner fa-spin fa-2x"/>
                        <p>Loading...</p>
                    </div>
                </t>
                <t t-elif="state.error">
                    <div class="alert alert-danger">
                        <strong>Error:</strong>
                        <t t-esc="state.error"/>
                    </div>
                </t>
                <t t-else="">
                    <div class="row">
                        <div class="col-md-6">
                            <canvas t-ref="chart1"/>
                        </div>
                        <div class="col-md-6">
                            <canvas t-ref="chart2"/>
                        </div>
                    </div>
                </t>
            </div>
        </div>
    </template>
</odoo>
