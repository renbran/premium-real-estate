<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <!-- Status Change Wizard Form View -->
        <record id="view_order_status_change_wizard_form" model="ir.ui.view">
            <field name="name">order.status.change.wizard.form</field>
            <field name="model">order.status.change.wizard</field>
            <field name="arch" type="xml">
                <form string="Change Order Status">
                    <sheet>
                        <div class="oe_title">
                            <h1>Change Order Status</h1>
                        </div>

                        <group>
                            <group>
                                <field name="order_id" readonly="1"/>
                                <field name="current_status_id" readonly="1"/>
                                <field name="new_status_id" required="1"/>
                            </group>
                            <group>
                                <!-- Conditional assignment fields -->
                                <field name="requires_documentation_user" invisible="1"/>
                                <field name="requires_commission_user" invisible="1"/>
                                <field name="requires_review_user" invisible="1"/>

                                <field name="documentation_user_id" invisible="not requires_documentation_user" required="requires_documentation_user"/>
                                <field name="commission_user_id" invisible="not requires_commission_user" required="requires_commission_user"/>
                                <field name="final_review_user_id" invisible="not requires_review_user" required="requires_review_user"/>
                            </group>
                        </group>

                        <group>
                            <field name="notes" placeholder="Add notes about this status change..." nolabel="1"/>
                        </group>
                    </sheet>

                    <footer>
                        <button name="change_status" string="Change Status" type="object" class="btn-primary"/>
                        <button string="Cancel" class="btn-secondary" special="cancel"/>
                    </footer>
                </form>
            </field>
        </record>

        <!-- Status Change Wizard Action -->
        <record id="action_order_status_change_wizard" model="ir.actions.act_window">
            <field name="name">Change Order Status</field>
            <field name="res_model">order.status.change.wizard</field>
            <field name="view_mode">form</field>
            <field name="target">new</field>
            <field name="view_id" ref="view_order_status_change_wizard_form"/>
        </record>
    </data>
</odoo>
